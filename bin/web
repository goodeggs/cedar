#!/bin/bash

AWS_REGION=`curl -s -m3 http://169.254.169.254/latest/dynamic/instance-identity/document | grep region | awk -F\" '{print $4}'`

/usr/bin/gen-cert ${AWS_REGION:-dev} > /app/.cedar.haproxy.pem

sudo haproxy -f /etc/cedar/haproxy.cfg &

export PORT=3000
eval exec $@
